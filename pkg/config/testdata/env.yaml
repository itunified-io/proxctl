version: "1"
kind: Env
metadata:
  name: test-env
  domain: example.com
spec:
  hypervisor:
    $ref: ./hypervisor.yaml
  networks:
    $ref: ./networks.yaml
  storage_classes:
    $ref: ./storage_classes.yaml
  cluster:
    $ref: ./cluster.yaml
